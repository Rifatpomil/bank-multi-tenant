services:
  db:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=bank_multitenant
    ports:
      - "3307:3306"
  rabbitmq:
    build: docker/rabbitmq/
    ports:
      - "5672:5672"
      - "15672:15672"
